const store = {
    state: {
        editStore:null
    },
    mutations: {
        setEditStore(state, store) {
            state.editStore = store
        },
    },
    actions: {}
}

export default store
